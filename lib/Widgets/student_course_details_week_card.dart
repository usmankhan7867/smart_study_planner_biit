import 'package:flutter/material.dart';

class StudentCourseDetailsWeekCard extends StatelessWidget {
  const StudentCourseDetailsWeekCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
